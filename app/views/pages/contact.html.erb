<h1>📞 Contact</h1>

<%# er suivi de tabulation pour exécuter du code sans l'afficher => <%  %> 

<%# pe suivi de tabulation pour exécuter du code en l'affichant  => <%=  %>

<form action="/contact" method="get">
  <input type="text" name="member" value="<%= params[:member] %>" placeholder="Who are you looking for?">
  <input type="submit">
</form>

<ol>
  <% @members.each do |member| %>
    <li><%= member.capitalize %></li>
  <% end %>
</ol>

<%= link_to 'Home', root_path %>
<%# le helper link_to va créer cette balise HTML => <a href="/">Accueil</a> %>
